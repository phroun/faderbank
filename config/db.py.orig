import pymysql
from pymysql.cursors import DictCursor

DB_CONFIG = {
    'host': 'localhost',
    'user': 'zebby_wiggle',
    'password': 'REDACTED',
    'database': 'zebby_wiggle',
    'charset': 'utf8mb4',
    'cursorclass': DictCursor
}

def get_db():
    return pymysql.connect(**DB_CONFIG)
